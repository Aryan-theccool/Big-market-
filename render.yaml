# Render Configuration for Inkspace Backend
# This tells Render how to deploy the backend server

services:
  - type: web
    name: inkspace-backend
    runtime: node
    buildCommand: echo "Building..."
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
